import { Moment } from 'moment/moment';
import { Field } from './field.model';

export class Location {
  address: string;
  longitude:number;
  latitude:number;
  floor: number;
  apartment: string;
}
